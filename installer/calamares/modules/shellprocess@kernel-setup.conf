---
# Populate /boot/ with vmlinuz and initramfs before the bootloader
# module runs grub2-mkconfig.  The ISO build installs kernel-core
# via dnf --installroot which often fails to run kernel-install
# scriptlets, leaving /boot/ empty.
dontChroot: false
script:
  - "/usr/share/lyrah/installer/setup-kernel.sh"
