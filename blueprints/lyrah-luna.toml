# Lyrah OS Luna Mode Blueprint
# Packages specific to Luna Mode gaming session

[extends]
base = "lyrah-base.toml"

[packages]
include = [
    "gamescope",
    # luna-ui: built from source in build-iso.yml phase 4 (not a dnf package yet)
    "steam",
    "wine-staging",
    "dxvk",
    "vkd3d",
    "protontricks",
    "protonup-qt",
]
